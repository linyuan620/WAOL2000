package skyray.waol2000.Core;

import skyray.waol2000.datamodel.FlowDetail;

/**
 * Created by Administrator on 2017/6/28.
 */

public class FlowDetailManage {
    public static void RunAction(FlowDetail d) {
//        IDataModel action = DataModuleCreator.CreatInstance(d.ActionType);
//        action = DBOperator.GetObject < IDataModel > (d.ActionId, action, "ActionId");
//        (action as FHMDataModel.Action).MeasureType = d.MeasureType;
//        if (action == null) {
//            throw new Exception("action is null");
//        }
//        IActionManage manage = (IActionManage) ActionManageCreator.CreatInstance(action.GetType().Name + "Manage");
//        manage.RunAction(action);
    }
}
